# 宿主业务配置示例
# 这个文件会覆盖插件的 config_defaults

# 数据库配置（覆盖默认值）
database:
  host: prod-db.example.com
  port: 3306
  name: production_db
  username: backup_user
  password: ${DB_PASSWORD}  # 可以使用环境变量

# 应用配置
app:
  log_level: debug
  timeout: 60
  max_connections: 100
